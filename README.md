# Storage

Workspace archive bundles (per-folder `tar.gz`), uploaded from Ray workspace `/home/ray/default`.

| File | Contents |
|------|----------|
| `folder_3dgrut.tar.gz` | `3dgrut/` (code) |
| `folder_3dgrut_runs.tar.gz` | `3dgrut_runs/` |
| `folder_blog_assets.tar.gz` | `blog_assets/` |
| `folder_data.tar.gz` | `data/` |
| `folder_gsplat.tar.gz` | `gsplat/` |
| `folder_gsplat_results.tar.gz` | `gsplat_results/` |
| `folder_ipynb_checkpoints.tar.gz` | `.ipynb_checkpoints/` |

Large files use **Git LFS**. Clone with Git LFS installed: `git lfs install && git clone ...`

The monolithic `default_workspace_pack_no360_*.tar.gz` (~2.5GB) was **not** uploaded (removed at source).
